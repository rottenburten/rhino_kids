# RevenueCat — iOS Public SDK API anahtarı (RevenueCat Dashboard → Project → API Keys).
# GERÇEK anahtarı .env dosyasına koy (bu dosya değil). .env GİT'E GİRMEZ (.gitignore).
# appl_ ile başlar.
VITE_REVENUECAT_IOS_KEY=appl_xxxxxxxxxxxxxxxxxxxxxxxxx
