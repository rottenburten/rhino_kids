# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code harness yerel dosyaları
.claude/scheduled_tasks.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets / credentials (API keys, RevenueCat, signing) — NEVER push
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
google-services.json
GoogleService-Info.plist
secrets*.json

# Android imzalama anahtarları — ASLA push etme (release keystore + şifreler).
# (android/.gitignore zaten build/, *.aab, local.properties'i kapsar; bu kök
#  seviye ek güvence keystore ve imza config'i içindir.)
*.jks
*.keystore
keystore.properties
android/keystore.properties
android/app/*.jks
android/app/*.keystore
android/key.properties
